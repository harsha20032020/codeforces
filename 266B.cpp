#include <bits/stdc++.h>
using namespace std;
int main()
{
    int test;
    string s;
    cin>>test>>s;
    while(test--)
    {
        for(int i=0;i<s.length();i++)
        {
            if(s[i]=='G')
        }
    }
}